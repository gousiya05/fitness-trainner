"""
ai-models/datasets/README.md
Dataset structure documentation.

The ML models are trained on SYNTHETIC data generated by training/train_models.py.
No external datasets are required.

If you want to train on real data, place CSV files here:
  datasets/workout_data.csv   — columns: age, weight, height, gender, goal, activity_level, fitness_level
  datasets/bmi_data.csv       — columns: weight, height, age, gender, bmi_category
  datasets/calorie_data.csv   — columns: weight, height, age, gender, exercise_type, duration_minutes, intensity, calories_burned

Then update training/train_models.py to load CSVs instead of generating random data.
"""
