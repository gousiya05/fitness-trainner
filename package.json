{
  "name": "ai-fitness-trainer",
  "version": "2.0.0",
  "description": "Full-stack AI Fitness Trainer — React + Node.js + FastAPI",
  "private": true,
  "scripts": {
    "setup": "npm install && npm install --prefix server && npm install --prefix client",
    "setup:py": "pip install -r ai-models/requirements.txt",
    "train": "python ai-models/training/train_models.py",

    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "dev:ai":     "uvicorn ai-models.api.main:app --reload --host 0.0.0.0 --port 8000",
    "dev": "concurrently -n SERVER,CLIENT,AI -c green,cyan,magenta \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:ai\"",

    "build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
